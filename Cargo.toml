[package]
name = "xpui-chrome"
description = "Themed components for xpui, painted from drawing primitives alone"
readme = "README.md"
keywords = ["ui", "embedded", "no-std", "eink", "xpui"]
categories = ["embedded", "gui", "no-std"]
version = "0.1.0"
edition = "2024"
license = "MIT"
authors = ["Thiago Holanda <unnamedd@gmail.com>"]
repository = "https://github.com/XPUI-Framework/xpui-chrome"

[lib]
name = "xpui_chrome"
crate-type = ["rlib"]

[dependencies]
# A git dependency, not a version: nothing is published, and a fresh clone of
# this repository has to build without its siblings on disk.
xpui = { git = "https://github.com/XPUI-Framework/xpui-framework", branch = "main" }

[dev-dependencies]
xpui = { git = "https://github.com/XPUI-Framework/xpui-framework", branch = "main", features = ["testing"] }

[workspace]
# `xtask` is the gate. A member rather than a separate workspace so one
# `cargo test` reaches its tests, and so the size ratchet counts its source
# like any other.
members = ["xtask"]

# What a `cargo build --release` of this repository alone produces.
#
# It is **not** what ships. A library's profile applies only when that library
# is the top of the build; when a firmware links it, the firmware's workspace
# sets the profile for everything in the graph. So this exists to make a local
# release build resemble the real one, not to control it.
#
# The repositories that do produce a flashed artifact — `xpui-rp2040`,
# `xpui-esp32`, `xpui-cpp` — use `opt-level = "z"` and say why there.
[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
